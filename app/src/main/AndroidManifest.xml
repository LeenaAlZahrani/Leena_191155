<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.Leena">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Leena191155">
        <activity android:name=".fetchUserFromSqlite"></activity>
        <activity android:name=".HomeSqlite" />
        <activity android:name=".fetchUserFromFirebase" />
        <activity android:name=".addToFireBase" />
        <activity android:name=".Home_Firebase" />
        <activity android:name=".Home">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WeatherResult" />
        <activity android:name=".WeatherController" />
        <activity android:name=".editSqliteUser" />
        <activity android:name=".ViewٍSavedUserSQlite" />
        <activity android:name=".editUser" />
        <activity android:name=".MainActivity" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>